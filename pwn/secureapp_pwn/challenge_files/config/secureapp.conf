service challenge
{
    disable = no
    socket_type = stream
    protocol = tcp
    wait = no
    user = root
    type = UNLISTED
    bind = 0.0.0.0
    port = 1002
    server = /app/secureapp
    per_source = 10
    rlimit_cpu = 20
}
