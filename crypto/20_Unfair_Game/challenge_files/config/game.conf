service challenge
{
    disable = no
    socket_type = stream
    protocol = tcp
    wait = no
    user = root
    type = UNLISTED
    bind = 0.0.0.0
    port = 1001
    server = /app/game
    per_source = 10
    rlimit_cpu = 20
}
